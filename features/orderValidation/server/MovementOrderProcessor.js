order.MovementOrderProcessor = function(){
  
}

order.MovementOrderProcessor.prototype.process = function(game, player, timeline, timelineEntry){
  /*
  var shipId = timeline.getContext().ship;
  var ship = this._shipService.getShipById(shipId);

  if ( ! shipId || ! ship)
    throw new Error("Timeline can not be associated with a ship");
  */
};

